# 🚗 ISUZU Stock & Test Drive Management System

ระบบจัดการสต็อกรถยนต์และการจองทดลองขับสำหรับตัวแทนจำหน่าย ISUZU

## 📋 คำอธิบายโปรเจกต์

ระบบจัดการแบบครบวงจรสำหรับตัวแทนจำหน่ายรถยนต์ ISUZU ประกอบด้วย:
- 📦 **การจัดการสต็อกรถยนต์** - เพิ่ม แก้ไข ลบ และติดตามสถานะรถยนต์ในสต็อก
- 🚙 **คิวทดลองขับ** - จัดการการจองและติดตามคิวทดลองขับรถยนต์
- 📅 **ปฏิทินการจอง** - มุมมองปฏิทินสำหรับการจองทดลองขับ
- 🎯 **จัดการอีเวนต์** - สร้างและจัดการอีเวนต์พิเศษ
- 📊 **Dashboard & Analytics** - สรุปและรายงานข้อมูล

---

## 🔧 เทคโนโลยีที่ใช้

### Frontend
- **Nuxt.js 2** - Vue.js Framework
- **Vue.js 2** - JavaScript Framework
- **Vuex** - State Management
- **TailwindCSS** - CSS Framework
- **Axios** - HTTP Client
- **Day.js** - Date/Time Library

### Backend API
- **NestJS** - Node.js Framework
- **PostgreSQL** - Database
- **TypeORM** - ORM
- **JWT** - Authentication

---

## 📦 ความต้องการของระบบ

- **Node.js** 14.0.0 หรือสูงกว่า
- **npm** 6.0.0 หรือสูงกว่า
- **Backend API Server** (isuzustock-management)

---

## 🚀 การติดตั้งและรันโปรเจกต์

### 1️⃣ ติดตั้ง Frontend

```bash
# Clone repository
git clone <repository-url>
cd testdrive-stock-system

# ติดตั้ง dependencies
npm install

# รัน development server
npm run dev
```

Frontend จะรันที่: **http://localhost:8080**

---

### 2️⃣ ติดตั้ง Backend API Server

```bash
# ไปที่โฟลเดอร์ backend (สมมติว่าอยู่ข้างๆ กัน)
cd ../isuzustock-management

# ติดตั้ง dependencies
npm install

# Setup database (ตาม README ของ backend)
# ...

# รัน backend server
npm run start:dev
```

Backend API จะรันที่: **http://localhost:3000**

---

### 3️⃣ เข้าสู่ระบบ

เปิดเบราว์เซอร์ไปที่: **http://localhost:8080/login**

**ข้อมูลเข้าสู่ระบบ:**
- Username: `admin`
- Password: `admin123`

---

## 📚 API Endpoints

ระบบใช้ API endpoints ต่อไปนี้:

### Authentication
- `POST /api/auth/login` - เข้าสู่ระบบ
- `GET /api/auth/me` - ดึงข้อมูลผู้ใช้ปัจจุบัน

### Test Drives (Queue)
- `GET /api/test-drives` - ดูรายการคิวทดลองขับทั้งหมด
- `POST /api/test-drives` - สร้างคิวใหม่
- `GET /api/test-drives/:id` - ดูรายละเอียดคิว
- `PATCH /api/test-drives/:id` - แก้ไขคิว
- `DELETE /api/test-drives/:id` - ลบคิว

### Stock Management
- `GET /api/stock/vehicles` - ดูรายการรถทั้งหมด
- `POST /api/stock` - เพิ่มรถใหม่
- `GET /api/stock/:id` - ดูรายละเอียดรถ
- `PATCH /api/stock/vehicles/:id/status` - อัพเดทสถานะรถ
- `DELETE /api/stock/vehicles/:id` - ลบรถ

### Events
- `GET /api/events` - ดูรายการอีเวนต์ทั้งหมด
- `POST /api/events` - สร้างอีเวนต์ใหม่
- `PATCH /api/events/:id` - แก้ไขอีเวนต์
- `DELETE /api/events/:id` - ลบอีเวนต์

### Staff
- `GET /api/staffs` - ดูรายการพนักงาน
- `GET /api/staffs/:id` - ดูข้อมูลพนักงาน

---

## 🎨 ฟีเจอร์หลัก

### 1. Dashboard
- สรุปข้อมูลรวมทั้งหมด
- กราฟและสถิติ
- รายการอัพเดทล่าสุด

### 2. Queue Management (คิวทดลองขับ)
- สร้าง/แก้ไข/ลบคิว
- กรองตามสถานะ (รอดำเนินการ, ยืนยันแล้ว, กำลังทดสอบ, เสร็จสิ้น)
- ค้นหาตามชื่อลูกค้า, เบอร์โทร
- จัดกลุ่มตามพนักงานขาย
- Export เป็น CSV/PDF

### 3. Stock Management (จัดการสต็อก)
- เพิ่มรถยนต์เข้าสต็อก
- แก้ไขข้อมูลรถ
- อัพเดทสถานะ (พร้อมใช้, ใช้งาน, บำรุงรักษา, ล็อกสำหรับอีเวนต์)
- กรองตามประเภท, สถานะ
- มุมมองแบบการ์ดและตาราง
- Upload จาก Excel

### 4. Events Management
- สร้างอีเวนต์พิเศษ
- กำหนดรถให้กับอีเวนต์
- ระบบตรวจสอบวันหมดอายุอีเวนต์
- คืนรถอัตโนมัติเมื่อหมดเวลา

### 5. Calendar View
- มุมมองปฏิทิน (เดือน, สัปดาห์, วัน)
- แสดงการจองทดลองขับ
- สีแยกตามสถานะ

---

## 🗂️ โครงสร้างโปรเจกต์

```
testdrive-stock-system/
├── pages/                    # Vue pages
│   ├── dashboard/           # Dashboard pages
│   │   ├── queue/          # Queue management
│   │   ├── stock/          # Stock management
│   │   ├── events.vue      # Events page
│   │   ├── calendar.vue    # Calendar view
│   │   ├── analytics.vue   # Analytics
│   │   └── settings.vue    # Settings
│   ├── login.vue           # Login page
│   └── index.vue           # Landing page
│
├── components/              # Reusable components
│   ├── QueueTable.vue      # Queue table component
│   ├── Sidebar.vue         # Navigation sidebar
│   ├── StatusBadge.vue     # Status badge
│   └── ...
│
├── plugins/                 # Nuxt plugins
│   ├── api.js              # API client
│   ├── auth-init.js        # Auth initialization
│   └── ...
│
├── store/                   # Vuex stores
│   ├── dashboard.js        # Dashboard state
│   ├── auth.js             # Auth state
│   └── stock.js            # Stock state
│
├── layouts/                 # Layouts
│   └── dashboard.vue       # Dashboard layout
│
├── middleware/              # Middleware
│   └── auth.js             # Auth middleware
│
└── nuxt.config.js          # Nuxt configuration
```

---

## 🛠️ Scripts

```bash
# Development
npm run dev              # รัน development server

# Production
npm run build            # Build สำหรับ production
npm run start            # รัน production server
npm run generate         # Generate static site
```

---

## 🔐 Authentication

ระบบใช้ JWT (JSON Web Token) สำหรับการยืนยันตัวตน:
- Token จะถูกเก็บใน Cookie (`auth-token`)
- Middleware ตรวจสอบ auth ก่อนเข้าหน้า dashboard
- Auto-refresh session เมื่อโหลดหน้าใหม่

---

## 🌐 Environment Variables

สร้างไฟล์ `.env` (optional - ใช้ default ได้):

```env
# API Configuration
API_BASE_URL=http://localhost:3000/api

# App Configuration
APP_NAME=ISUZU Stock Management
APP_URL=http://localhost:8080
```

---

## 📝 การใช้งาน

### เพิ่มคิวทดลองขับ
1. ไปที่ **Queue Management**
2. คลิก **"เพิ่มคิวใหม่"**
3. กรอกข้อมูลลูกค้า, เลือกรถ, กำหนดวันเวลา
4. คลิก **"บันทึก"**

### เพิ่มรถเข้าสต็อก
1. ไปที่ **Stock Management**
2. คลิก **"เพิ่มรถใหม่"**
3. กรอกข้อมูลรถ (รุ่น, ทะเบียน, สี, ปี, ฯลฯ)
4. คลิก **"บันทึก"**

### สร้างอีเวนต์
1. ไปที่ **Events Management**
2. คลิก **"สร้างอีเวนต์ใหม่"**
3. กรอกข้อมูลอีเวนต์, เลือกรถที่ต้องการ
4. คลิก **"บันทึก"**

---

## 🐛 การแก้ไขปัญหา

### Frontend รันไม่ขึ้น
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### ไม่สามารถเข้าสู่ระบบได้
- ตรวจสอบว่า Backend API รันอยู่ที่ port 3000
- ตรวจสอบ credentials: admin / admin123
- เปิด Console (F12) ดู error messages

### ข้อมูลไม่แสดง
- ตรวจสอบว่า Backend API ทำงานปกติ
- เปิด Network tab ใน DevTools ดู API requests
- ตรวจสอบ Console สำหรับ errors

---

## 📄 License

This project is private and proprietary.

---

## 👥 Contributors

- **Development Team** - Full-stack Development
- **ISUZU Thailand** - Requirements & Specifications

---

## 📞 Contact & Support

สำหรับปัญหาหรือคำถาม กรุณาติดต่อ:
- Email: support@example.com
- Issue Tracker: [GitHub Issues]

---

**หมายเหตุ:** โปรเจกต์นี้พัฒนาเพื่อการศึกษาและใช้งานภายใน ห้ามเผยแพร่หรือใช้งานเชิงพาณิชย์โดยไม่ได้รับอนุญาต

---

**Last Updated:** November 2025
**Version:** 1.0.0
